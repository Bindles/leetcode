
a=[1,2]
a.slice(-1)
a.slice(-1)[0]

p=console.log.bind(console)
l=console.log.bind(console)
const numbers = [1, 2, 3, 4, 5];
numbers.map(number => number * 10); // No parentheses needed

fname = 'nick'
p(
  username = fname.toUpperCase()  )
  
p(username)

str = "The Dog Jumped Over the Fence";
p (
  str.includes("the"),
  str.includes("dog"),
  str.toLowerCase().includes("fence"),
)
for (i=0; i<5; i++) {  
  p(i)  
}


let theArray = [1, 2, 3, 4, 5];
theArray = [-1,0,...theArray, 6,7,];
console.log(theArray);
a=[1,2]
var text = 'uololooo';

// With ES6
[...text].forEach(c => a[a.length] = c)
p(a)